"""Master Layer — canonical company representation (Sprint 1). Provider-independent.

`master_companies` is the OFFICIAL canonical collection. `companies_master` is legacy
(in deprecation). This package builds master_companies from the Normalized layer via
explainable entity resolution + non-destructive merge, plus the structural ownership graph.
"""
